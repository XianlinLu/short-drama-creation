interface:
  display_name: "Short Drama Creation"
  short_description: "原创短剧、连续视频延长与自动多语言创作"
  default_prompt: "Use $short-drama-creation to turn my character reference into an original short drama, then continuously extend the initial video to the duration specified in my prompt."

policy:
  allow_implicit_invocation: true
